# Floating-Point Representation Tasks

Convert each binary number to floating-point representation.

For each question, use:

- a **16-bit mantissa**, including the sign bit; and
- an **8-bit exponent**.

Show the sign bit, mantissa and exponent in your answer.

---

## Task One

Convert ==0.001011<sup>2</sup>== to floating-point representation.

| Sign bit (1 bit) | Mantissa (15 bits) | Exponent (8 bits) |
| --- | --- | --- |
|  |  |  |

---

## Task Two

Convert ==100.0011<sup>2</sup>== to floating-point representation.

| Sign bit (1 bit) | Mantissa (15 bits) | Exponent (8 bits) |
| --- | --- | --- |
|  |  |  |

---

## Task Three

Convert ==0.00001011<sup>2</sup>== to floating-point representation.

| Sign bit (1 bit) | Mantissa (15 bits) | Exponent (8 bits) |
| --- | --- | --- |
|  |  |  |

---

## Task Four

Convert ==-10.0101<sup>2</sup>== to floating-point representation.

| Sign bit (1 bit) | Mantissa (15 bits) | Exponent (8 bits) |
| --- | --- | --- |
|  |  |  |

---

## Task Five

Convert ==-0.010101<sup>2</sup>== to floating-point representation.

| Sign bit (1 bit) | Mantissa (15 bits) | Exponent (8 bits) |
| --- | --- | --- |
|  |  |  |
