# Sub-programs: Putting It All Together

Complete the lessons on sub-programs, parameter passing and scope before
starting these tasks.

Complete each task in Python and record the requested evidence in your
workbook.

## Task 1

```python linenums="1"
def calculate_pay(hours, hourly_rate):
    pay = hours * hourly_rate
    return pay

def display_pay(employee_name, pay):
    print(employee_name, "earned £", format(pay, ".2f"))

name = "Alex"
total_pay = calculate_pay(12, 9.50)
display_pay(name, total_pay)
```

Identify:

1. the procedure and the function
2. the formal parameters
3. the actual parameters
4. the inputs and output of `calculate_pay`
5. the local variables

In your workbook, write your answers to questions 1–5 and place a screenshot
of the supplied code beside them.

## Task 2

A teacher needs a program that calculates a pupil's percentage and displays the
result. The main program has already been written:

```python linenums="1"
pupil_name = "Aisha"
pupil_mark = 42
maximum_mark = 50

pupil_percentage = calculate_percentage(pupil_mark, maximum_mark)
display_result(pupil_name, pupil_percentage)
```

Add these two sub-programs above the main program:

1. `calculate_percentage(mark, total)` must calculate and return the percentage.
2. `display_result(name, percentage)` must display the pupil's name and
   percentage.

Test your program with the following data:

| Pupil name | Mark | Maximum mark |
|---|---:|---:|
| Aisha | 42 | 50 |

> The expected output is: **Aisha scored 84.0 %**

In your workbook, include:

1. a screenshot showing all of your completed code
2. a screenshot of the program output showing `Aisha scored 84.0 %`


## Task 3

Convert this program into a modular solution:

```python linenums="1"
basketball_player = input("Type basketball player's name: ")
season_points = int(input("Season points scored: "))
player_height = float(input("Player height: "))

if season_points > 100 and player_height >= 2.00:
    all_star = True
else:
    all_star = False

print(basketball_player)
print("All Star:", all_star)
```

Create these three sub-programs:

1. An input sub-program that asks for the player's name, season points and
   height, then returns all three values.
2. A processing function that receives the season points and height as
   parameters. It must return `True` when the player has more than 100 points
   and is at least 2 metres tall. Otherwise, it must return `False`.
3. An output procedure that receives the player's name and All Star result as
   parameters, then displays both values.

After the three sub-programs, write the main program. It must call the input
sub-program, pass the returned points and height to the processing function,
and pass the player's name and returned result to the output procedure. A
sub-program must only use values received through its parameters or created
inside that sub-program.

Run the program once. In your workbook, include:

1. a screenshot showing all of your completed code
2. a screenshot showing the inputs you entered and the program output

??? example "Extension Tasks"

    Complete these tasks if you finish the three core tasks.

    ### Extension 1: Trace Data Flow

    Before running the program, trace each line and write the values of
    `original_price`, `discount` and `final_price` in a table. Write the exact
    output you expect beneath the table. Then run the program and compare its
    output with your prediction.

    ```python linenums="1"
    def calculate_discount(price, percentage):
        saving = price * percentage / 100
        return saving

    def calculate_sale_price(price, saving):
        sale_price = price - saving
        return sale_price

    original_price = 80.00
    discount = calculate_discount(original_price, 15)
    final_price = calculate_sale_price(original_price, discount)
    print("Sale price: £", final_price)
    ```

    In your workbook, include:

    1. a screenshot showing all of the code
    2. a screenshot showing the program output

    ### Extension 2: Total Pay

    Convert this program into a modular solution:

    ```python linenums="1"
    employee_names = []
    wages = []
    total_pays = []

    for index in range(5):
        employee_names.append(input("Enter employee name: "))
        wages.append(float(input("Enter employee wage: ")))
        total_pays.append(wages[index] + 50.90)

    print("Employee Name\tTotal Pay")
    for index in range(5):
        print(employee_names[index], "\t£", total_pays[index])
    ```

    !!! note "Remember"

        `\t` uses a backslash and inserts a **tab**. It adds space between the
        employee name and total pay to help arrange the output into columns.

    Create these three sub-programs:

    1. An input sub-program that reads five employee names and wages, stores
       them in two lists, and returns both lists.
    2. A processing function that receives the wages list as a parameter, adds
       `50.90` to each wage, and returns a new total-pays list.
    3. An output procedure that receives the employee-names and total-pays
       lists as parameters and displays the table.

    After the three sub-programs, write the main program. It must call each
    sub-program in order and store their returned values. A sub-program must
    not directly read or change a variable created in the main program; pass
    every required value as a parameter.

    Run the program with five employees. In your workbook, include:

    1. a screenshot showing all of your completed code
    2. a screenshot showing the five employees in the output table
