# Pre-defined Functions: Putting It All Together

Complete the lesson on pre-defined functions before starting these tasks.

Complete each task in Python and record the requested evidence in your
workbook.

## Task 1

```python linenums="1"
product_code = "AB1234"

department = product_code[0:2]
item_number = int(product_code[2:6])
first_character_code = ord(product_code[0])
next_character = chr(first_character_code + 1)
remainder = item_number % 5

print(department)
print(item_number)
print(first_character_code)
print(next_character)
print(remainder)
```

Identify:

1. the two substrings used in the program
2. the value assigned to `department`
3. the value assigned to `item_number`
4. the purpose of `ord()` and `chr()`
5. the value assigned to `remainder`
6. the exact output produced by the program

In your workbook, write your answers to questions 1–6.

## Task 2

A cinema uses an eight-character booking reference. The first three characters
identify the film and the final five characters contain the booking number.
The main program has already been written:

```python linenums="1"
booking_reference = "DUN02468"

film_code = booking_reference[0:3]
booking_number_text = booking_reference[3:8]

print("Film code:", film_code)
print("Booking number:", booking_number)
print("Even booking number:", is_even)
```

Between the substring statements and the output statements, add code that:

1. converts `booking_number_text` to an integer and stores it in
   `booking_number`
2. uses modulus to check whether `booking_number` is even and stores the
   Boolean result in `is_even`

Test your program with the following data:

| Booking reference | Expected film code | Expected booking number | Expected even result |
|---|---|---:|---|
| `DUN02468` | `DUN` | `2468` | `True` |

> The expected output is:
>
> ```text
> Film code: DUN
> Booking number: 2468
> Even booking number: True
> ```

In your workbook, include:

1. a screenshot showing all of your completed code
2. a screenshot of the program output showing the three expected results

## Task 3

A school stores a pupil result as a single code. A code such as `MIA073B`
contains:

- the pupil identifier in characters 1–3
- the mark in characters 4–6
- the class letter in character 7

Write a program that:

1. asks the user to enter a seven-character result code
2. uses substrings to extract the pupil identifier, mark and class letter
3. converts the mark from a string to an integer
4. converts the class letter to its character code
5. calculates the remainder when the mark is divided by `10`
6. converts the next character code back to a character using `chr()`
7. displays all the calculated results with suitable labels

Test your program with the following data:

| Result code | Pupil identifier | Mark | Class code | Remainder | Next letter |
|---|---|---:|---:|---:|---|
| `MIA073B` | `MIA` | `73` | `66` | `3` | `C` |

Run the program using the test data. In your workbook, include:

1. a screenshot showing all of your completed code
2. a screenshot showing the input and all five expected output values

??? example "Extension Tasks"

    Complete this task if you finish the three core tasks.

    ### The Username Generator

    A school creates a username from a pupil's details. Write a program that:

    1. asks for the pupil's first name, surname and year number
    2. extracts the first character of the first name
    3. extracts the first four characters of the surname
    4. converts the year number to an integer
    5. uses modulus to calculate the remainder when the year number is divided
       by `2`
    6. joins the substrings and year number to create and display a username
    7. displays the character code of the username's first character

    Test the program using `Aisha`, `Khan` and `2026`. The username should be
    `AKhan2026`, and the remainder should be `0`.

    In your workbook, include:

    1. a screenshot showing all of your completed code
    2. a screenshot showing the username, remainder and character code
